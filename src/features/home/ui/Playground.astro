---
import { Image } from "astro:assets"
import figuresSvg from "~/assets/images/figures.svg"
import ferrisImage from "~/assets/images/webp/ferris.webp"
---

<section
  id="playground"
  class="relative flex flex-col justify-center items-center w-full py-20 px-6 bg-dark-fg"
>
  <div class="w-full h-12 absolute bottom-full bg-repeat-x" style={`background-image: url(${figuresSvg.src})`}></div>

  <h2 class="text-5xl md:text-6xl font-extrabold text-center mb-12">Prueba Rust en tu navegador</h2>

  <div class="max-w-6xl w-full flex flex-col md:flex-row items-center gap-10">
    <Image
      class="w-full md:w-1/2 max-w-md h-auto z-20"
      src={ferrisImage}
      alt="Ferris, la mascota de Rust"
      fetchpriority="high"
    />

    <p class="text-center md:text-left w-full md:w-1/2 text-pretty leading-relaxed">
      <span class="block font-bold text-xl mb-2 text-center">¡Aprende Rust sin instalar nada!</span>
      Explora lecciones interactivas y experimenta con código en un playground integrado, directamente
      desde tu navegador. 🚀
    </p>
  </div>

  <div class="w-full rotate-180 h-12 absolute top-full bg-repeat-x" style={`background-image: url(${figuresSvg.src})`}></div>
</section>
